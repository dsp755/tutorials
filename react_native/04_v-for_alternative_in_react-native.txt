1. Пример итерации элемента:

        <View>
          { todos.map(todo => <Text key={todo.id}>{todo.title}</Text>) }
        </View>

Значение key мы обязательно должны указывать в качестве аттрибута того элемента, который мы итерируем.

Вместо Text мы также можем передавать любой компонент.


